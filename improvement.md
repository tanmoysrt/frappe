
## 2026-06-07 14:20 — Phase 19 done (architecture options live in common site config)
- `frappe.get_common_conf(key, default)` (frappe/config.py): reads ONLY common_site_config.json — never site config, works without site init, 60s process cache; `patch_common_conf(**knobs)` context manager for tests (mirror of change_settings)
- Flipped readers (one commit per group): use_async_db + db_pool_size/recycle/idle/acquire (database/__init__.py, mariadb/aio.py), queue_backend (background_jobs.py), use_node_realtime (realtime.py), in_process_scheduler (scheduler.py). socketio_port read sites already common-sourced; boot.py only forwards display values — untouched
- app.py serve knobs (asgi_pool_size, asgi_limit_concurrency, webserver_port, asgi_thread_stack, malloc_trim_interval, use_tcmalloc) already read common config with env override since earlier phase — no change needed; documented order env > common > default
- Per-site architecture keys now IGNORED: once-per-process boot warning lists them (verified live: site `use_async_db: 0` ignored, common value wins, warning fires)
- Live values: all knobs already in common_site_config.json; both site_config.json files clean — no migration needed
- Tests: 6 new in test_config (read/missing-file/malformed-json/no-site-context/site-isolation/patch nesting) + queue/realtime/db tests switched from frappe.conf pinning to patch_common_conf; new regression: site-level queue_backend ignored
- Suites: unit 69/69; test_config 7/7, arq 6/6 (+1 new), sqlite_queue 11/11, realtime_server 7/7, scheduler_task 4/4, async_db mariadb 10/10 + sqlite 10/10, aio_orm 18/18; rq_job 11/12 with worker running (test_memory_usage fails IDENTICALLY on clean tree — worker RSS env drift 56 vs 53 MB baseline, pre-existing); test_background_jobs cleanup crash pre-existing (verified clean tree)
- Architecture-knob table (all in common_site_config.json): use_async_db, queue_backend, use_node_realtime, socketio_port, db_pool_size/recycle/idle_timeout/acquire_timeout, in_process_scheduler, cache_backend, sqlite_engine, asgi_pool_size, asgi_limit_concurrency, asgi_thread_stack, malloc_trim_interval, use_tcmalloc, webserver_port. Stays per-site: enable_scheduler, allow_tests, developer_mode, db credentials, use_mysqlclient
- Benchmark phase19: **121.2 rps, p50 78ms, RSS 166/232 MB** — flat vs phase18 (115.8), as expected (config reads cached/boot-time)
